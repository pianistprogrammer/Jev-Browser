{
  "name": "jev-browser",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace @jev/api\" \"npm run dev --workspace @jev/web\"",
    "build": "npm run build --workspace @jev/contracts && npm run build --workspace @jev/api && npm run build --workspace @jev/web",
    "test": "npm run test --workspace @jev/api && npm run test --workspace @jev/web",
    "typecheck": "npm run build --workspace @jev/contracts && npm run typecheck --workspace @jev/api && npm run typecheck --workspace @jev/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
